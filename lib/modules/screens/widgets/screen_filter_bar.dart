import 'package:flutter/material.dart';

class ScreenFilterBar extends StatelessWidget {
  const ScreenFilterBar({super.key});

  @override
  Widget build(BuildContext context) {
    return const SizedBox.shrink(); // screen_filter_bar skeleton widget.
  }
}
