import 'package:flutter/material.dart';

/// يمثل ProfileMenuItem كجزء من بنية المشروع ويجمع المسؤولية الخاصة به في مكان واحد.
class ProfileMenuItem extends StatelessWidget {
  const ProfileMenuItem({super.key});

  @override
  Widget build(BuildContext context) {
    return const SizedBox.shrink(); // profile_menu_item skeleton widget.
  }
}
