import 'package:flutter/material.dart';

/// يمثل PermissionTree كجزء من بنية المشروع ويجمع المسؤولية الخاصة به في مكان واحد.
class PermissionTree extends StatelessWidget {
  const PermissionTree({super.key});

  @override
  Widget build(BuildContext context) {
    return const SizedBox.shrink(); // permission_tree skeleton widget.
  }
}
