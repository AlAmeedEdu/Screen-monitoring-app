import 'package:flutter/material.dart';

class SchoolCard extends StatelessWidget {
  const SchoolCard({super.key});

  @override
  Widget build(BuildContext context) {
    return const SizedBox.shrink(); // school_card skeleton widget.
  }
}
