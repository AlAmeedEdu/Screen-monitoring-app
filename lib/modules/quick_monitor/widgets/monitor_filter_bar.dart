import 'package:flutter/material.dart';

/// يمثل MonitorFilterBar كجزء من بنية المشروع ويجمع المسؤولية الخاصة به في مكان واحد.
class MonitorFilterBar extends StatelessWidget {
  const MonitorFilterBar({super.key});

  @override
  Widget build(BuildContext context) {
    return const SizedBox.shrink(); // monitor_filter_bar skeleton widget.
  }
}
