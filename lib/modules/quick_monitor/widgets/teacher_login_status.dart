import 'package:flutter/material.dart';

class TeacherLoginStatus extends StatelessWidget {
  const TeacherLoginStatus({super.key});

  @override
  Widget build(BuildContext context) {
    return const SizedBox.shrink(); // teacher_login_status skeleton widget.
  }
}
