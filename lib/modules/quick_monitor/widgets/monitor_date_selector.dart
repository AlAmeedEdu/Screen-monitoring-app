import 'package:flutter/material.dart';

/// يمثل MonitorDateSelector كجزء من بنية المشروع ويجمع المسؤولية الخاصة به في مكان واحد.
class MonitorDateSelector extends StatelessWidget {
  const MonitorDateSelector({super.key});

  @override
  Widget build(BuildContext context) {
    return const SizedBox.shrink(); // monitor_date_selector skeleton widget.
  }
}
